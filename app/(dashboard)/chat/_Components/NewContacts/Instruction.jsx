import React from "react";
import styles from "./NewContacts.module.css"
const Instruction = () => {
  return <div className={styles.instruction}>Want to add a new person to your connections, search through their email, if exists add them by clicking the + button.</div>;
};

export default Instruction;
